#include <stdio.h>
#include <stdlib.h>

int main()
{
    float Celsius;
    printf("enter temperature in Celsius Celsius = ");
    scanf("%f",&Celsius );
    printf("temperature in Fahrenheit = %f", (Celsius*(9/5)+32) );
}
