#include <stdio.h>
#include <stdlib.h>
#include<math.h>
unsigned long long fact(int n);

int main()
{int n;
scanf("%d",&n);
     printf("factorial of %d = %d", n, fact(n));

    return 0;
}
unsigned long long fact(int n){
    if(n==0){
        return 1;
    }
    return(n*fact(n-1));
}
