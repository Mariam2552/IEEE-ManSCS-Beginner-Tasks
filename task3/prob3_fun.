#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int max(int n1,int n2);
int main()
{
    int num1,num2;
    scanf("%d %d",&num1,&num2);
    printf("\nMaximum = %d\n",max(num1,num2));
    printf("\nMaximum = %d\n",min(num1,num2));

    return 0;
}
int max(int n1,int n2){
  return((n1>n2 ? n1 :n2));
}
int min(int n1,int n2){
  return((n1<n2 ? n1 :n2));
}
