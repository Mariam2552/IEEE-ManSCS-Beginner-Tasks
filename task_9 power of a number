#include <stdio.h>
#include <stdlib.h>
#include<math.h>

int main()
{
    double base,exp;
    printf("enter base = ");
    scanf("%lf",& base);
    printf("enter exponent = ");
    scanf("%lf",&exp);
    printf(" %lf ^ %lf =%lf", base,exp, pow(base, exp) );
}
