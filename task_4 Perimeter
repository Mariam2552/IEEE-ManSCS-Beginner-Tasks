#include <stdio.h>
#include <stdlib.h>

int main()
{
    float length,width;
     printf("Enter length");
    scanf("%f",& length);
     printf("Enter width");
    scanf("%f",& width);
    printf("Perimeter of rectangle = %f", 2*(length+width));

    return 0;
}
