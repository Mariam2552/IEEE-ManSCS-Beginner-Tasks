#include <stdio.h>
#include <stdlib.h>

int main()
{
    float length,width;
     printf("Enter length ");
    scanf("%f",& length);
     printf("Enter width ");
    scanf("%f",& width);
    printf("Area of rectangle = %f ", length*width);

    return 0;
}
